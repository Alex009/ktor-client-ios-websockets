pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "ktor_client_websockets"
include(":androidApp")
include(":shared")